/** Must stay in sync with API cache + migrations. Bump to invalidate stored analyses. */
export const PIPELINE_VERSION = "11";
export const SCHEMA_VERSION = "2";

export const CACHE_MAX_AGE_DAYS = 15;
